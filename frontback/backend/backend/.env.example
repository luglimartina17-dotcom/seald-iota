# â”€â”€ Backend (.env) â”€â”€
# Copia questo file come .env e compila i valori

# URL RPC della rete IOTA (testnet)
IOTA_RPC=https://api.testnet.iota.cafe

# API Key per proteggere gli endpoint sensibili (scegli una stringa segreta)
API_KEY=la-tua-api-key-segreta

# Origini consentite per CORS (frontend Vite)
ALLOWED_ORIGINS=http://localhost:5173
